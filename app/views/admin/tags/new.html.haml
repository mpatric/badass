%h2 New tag
%fieldset.admin
  = form_for @tag, :url => admin_tags_path do |form|
    = render :partial => '/admin/errors', :locals => { :errors => @tag.errors }
    = render :partial => 'form', :object => form
    .actions
      = form.submit 'Create', :class => 'button'